// const companySlider = document.querySelector('.company-slider');
// const prevButton = document.querySelector('.prev-btn');
// const nextButton = document.querySelector('.next-btn');

// let currentIndex = 0;

// function slideCompanies() {
//   const offset = -(currentIndex * 250); // Adjust based on card width
//   companySlider.style.transform = `translateX(${offset}px)`;
// }

// prevButton.addEventListener('click', () => {
//   currentIndex = Math.max(0, currentIndex - 1);
//   slideCompanies();
// });

// nextButton.addEventListener('click', () => {
//   const maxIndex = companySlider.childElementCount - 1;
//   currentIndex = Math.min(maxIndex, currentIndex + 1);
//   slideCompanies();
// });
